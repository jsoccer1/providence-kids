// Copy this file to config.js and fill in your Supabase project values.
// (config.js is gitignored. On GitHub Pages it is generated automatically
// from repository secrets by .github/workflows/deploy.yml.)
//
// Find these in Supabase: Project Settings → API.
// The anon (public) key is safe to ship to the browser — access is
// controlled by Row Level Security policies, not by hiding the key.

window.SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
